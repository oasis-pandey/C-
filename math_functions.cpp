#include <iostream>
#include <cmath>
int main(){
    int x=2;
    int y=3;
    int z;
    // z= std::max(x,y);
    // z= std::min(x,y);
    // z=abs(-5);
    // z= pow(x,y);
    // z= round (3.14);
    // z=sqrt(9);
    // z=ceil(3.14); rounds off to greater
    // z=floor(3.99999); round off to lower
    std::cout<<z;

    return 0;
}